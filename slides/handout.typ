#import "@preview/polylux:0.4.0": enable-handout-mode
#import "presentation.typ": slides

// Builds the presentation in handout mode, i.e. without slide breaks.
// #enable-handout-mode(true) // breaks layout :(
#slides
